name: Health Live Development Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: health-live-development
  cancel-in-progress: true

jobs:
  deploy_live:
    runs-on: ubuntu-24.04
    timeout-minutes: 18
    environment:
      name: health-production
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      HEALTH_PRODUCTION_D1_DATABASE_ID: ${{ secrets.HEALTH_PRODUCTION_D1_DATABASE_ID }}
      HEALTH_PREVIEW_D1_DATABASE_ID: ${{ secrets.HEALTH_PREVIEW_D1_DATABASE_ID }}
      APPLICATION_MANAGEMENT_PRODUCTION_ORIGIN: ${{ vars.APPLICATION_MANAGEMENT_PRODUCTION_ORIGIN }}
      HEALTH_PRODUCTION_ORIGIN: ${{ vars.HEALTH_PRODUCTION_ORIGIN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"
      - name: Detect live readiness
        id: ready
        shell: bash
        run: |
          missing=()
          for key in CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID HEALTH_PRODUCTION_D1_DATABASE_ID HEALTH_PREVIEW_D1_DATABASE_ID APPLICATION_MANAGEMENT_PRODUCTION_ORIGIN HEALTH_PRODUCTION_ORIGIN; do
            [[ -n "${!key:-}" ]] || missing+=("$key")
          done
          if (( ${#missing[@]} )); then
            echo "deploy=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Live publish skipped; missing: ${missing[*]}"
          else
            echo "deploy=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Install
        if: ${{ steps.ready.outputs.deploy == 'true' }}
        run: npm ci --no-audit --no-fund
      - name: Materialize live config
        if: ${{ steps.ready.outputs.deploy == 'true' }}
        run: node scripts/prepare-cloudflare-production.mjs
      - name: Apply D1 migrations
        if: ${{ steps.ready.outputs.deploy == 'true' }}
        run: npx wrangler d1 migrations apply health-care-production-db --remote --config wrangler.cloudflare.production.jsonc
      - name: Build
        if: ${{ steps.ready.outputs.deploy == 'true' }}
        env:
          CLOUDFLARE_VITE_WRANGLER_CONFIG_PATH: wrangler.cloudflare.production.jsonc
        run: npm run build
      - name: Deploy live
        if: ${{ steps.ready.outputs.deploy == 'true' }}
        run: npx wrangler deploy
      - name: Summary
        if: ${{ always() }}
        run: |
          echo "Health Standalone Development Mode"
          echo "main is the live development source"
