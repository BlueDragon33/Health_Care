name: Patch Secure Vault Backup Recovery V1

on:
  push:
    branches:
      - feature/secure-vault-backup-recovery-v1
    paths:
      - .secure-vault-backup-recovery-v1-trigger

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/secure-vault-backup-recovery-v1
      - name: Integrate Secure Vault Backup Recovery V1
        run: python .github/scripts/patch-secure-vault-backup-recovery-v1.py
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/suc-khoe-tre/health-secure-vault.ts app/suc-khoe-tre/secure-vault-session.tsx app/suc-khoe-tre/health-framework.tsx app/suc-khoe-tre/page.tsx package.json
          git diff --cached --quiet || git commit -m "feat(vault): integrate encrypted backup recovery v1"
          git push
